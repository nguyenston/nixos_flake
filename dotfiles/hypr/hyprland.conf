
#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#

autogenerated = 0 # remove this line to remove the warning

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=desc:BNQ BenQ GW2480 ET85L06525SL0,preferred,1920x-1080,1
monitor=desc:Audio Processing Technology  Ltd MNN demoset-1,preferred,0x-1080,1
monitor=desc:California Institute of Technology 0x1410,preferred,0x0,1.5
monitor=,preferred,auto,auto


# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = ags -q && nohup ags 0<&- &>/dev/null &
exec-once = wpaperd -d ; ~/.config/hypr/monitor_change_script.sh
exec-once = hypridle
exec-once = inhibit-lid-switch.sh
exec-once = nm-applet
exec-once = fcitx5

env = HYPRLAND_LOG_WLR,1

env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland

env = GDK_BACKEND,wayland,x11
env = QT_QPA_PLATFORM,wayland;xcb
env = CLUTTER_BACKEND,wayland

env = LIBVA_DRIVER_NAME,nvidia
# env = GBM_BACKEND,nvidia-drm
# env = WLR_NO_HARDWARE_CURSORS,1
env = WLR_DRM_DEVICES,/dev/dri/card2:/dev/dri/card1

env = VISUAL,nvim
env = EDITOR,nvim
env = BROWSER,firefox-beta
env = DEFAULT_BROWSER,firefox-beta

# env = HYPRCURSOR_THEME,rose-pine-hyprcursor
# env = HYPRCURSOR_SIZE,24
env = XCURSOR_THEME,Bibata-Modern-Classic
env = XCURSOR_SIZE,10
# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 2

    touchpad {
        natural_scroll = yes
	      scroll_factor = 0.5
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 4
    gaps_out = 8
    border_size = 2
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    layout = dwindle
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 5

    # drop_shadow = no
    # shadow_range = 4
    # shadow_render_power = 3
    # col.shadow = rgba(1a1a1aee)

    blur {
        enabled = true
        size = 4
        new_optimizations = true
    }
}

animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default, slidevert
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_status = slave
    new_on_active = after
}

group {
    col.border_active = rgb(f5ec71) rgb(f5be71) 45deg

    groupbar {
        font_family = JetBrainsMono
        col.active = rgb(cf9748)
    }
}

misc {
    disable_hyprland_logo = true
    disable_splash_rendering = true
    # mouse_move_enables_dpms = true
}

debug {
    disable_logs = false
    # enable_stdout_logs = true
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = on
}

# unscale XWayland
# xwayland {
#   force_zero_scaling = true
# }
# toolkit-specific scale
# env = GDK_SCALE,2
# env = XCURSOR_SIZE,32


# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device {
}

cursor {
    no_hardware_cursors = true
}

# hyprland distinct workspaces
plugin {
    split-monitor-workspaces {
        count = 6
    }

    hyprbars {
        # example config
        bar_height = 20

        # example buttons (R -> L)
        # hyprbars-button = color, size, on-click
        hyprbars-button = rgb(ff4040), 10, 󰖭, hyprctl dispatch killactive
        hyprbars-button = rgb(eeee11), 10, , hyprctl dispatch fullscreen 1
    }
}
# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more

windowrule = float, Rofi
windowrule = animation none, Rofi
windowrule = float,title:(Quick Format Citation)
windowrulev2 = idleinhibit always,title:(inhibitidle)
windowrulev2 = float,title:(inhibitidle)
windowrulev2 = size 10% 10%,title:(inhibitidle)
windowrulev2 = pin,title:(inhibitidle)

# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = ALT

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
# bind = $mainMod, Return, exec, __EGL_VENDOR_LIBRARY_FILENAMES="/run/opengl-driver/share/glvnd/egl_vendor.d/50_mesa.json" alacritty
bind = $mainMod, Return, exec, kitty
bind = $mainMod CTRL, Return, exec, kitty -T 'inhibitidle' cmatrix -u 10
bind = $mainMod SHIFT, O, killactive, 
bind = $mainMod SHIFT, P, exit,
bind = $mainMod, Space, togglefloating, 
bind = $mainMod, D, exec, ~/.config/rofi/launch.sh
bind = $mainMod SHIFT, P, pin, # pin active window
bind = $mainMod, P, exec, ~/.config/rofi/power_menu/power_menu.sh hyprland # logout menu
bind = $mainMod, semicolon, togglesplit, # dwindle

# Screenshot
bind = $mainMod SHIFT, C, exec, grim -g "$(slurp)" - | wl-copy -t image/png

# Color picker
bind = $mainMod SHIFT, X, exec, hyprpicker -a


# toggle fullscreen
bind = $mainMod, F, fullscreen

# Move focus with mainMod + arrow keys
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

# Move windows
bind = $mainMod SHIFT, H, movewindow, l
bind = $mainMod SHIFT, L, movewindow, r
bind = $mainMod SHIFT, K, movewindow, u
bind = $mainMod SHIFT, J, movewindow, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, Q, workspace, 1
bind = $mainMod, W, workspace, 2
bind = $mainMod, E, workspace, 3
bind = $mainMod, R, workspace, 4
bind = $mainMod, T, workspace, 5
bind = $mainMod, Y, workspace, 6

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, Q, movetoworkspacesilent, 1
bind = $mainMod SHIFT, W, movetoworkspacesilent, 2
bind = $mainMod SHIFT, E, movetoworkspacesilent, 3
bind = $mainMod SHIFT, R, movetoworkspacesilent, 4
bind = $mainMod SHIFT, T, movetoworkspacesilent, 5
bind = $mainMod SHIFT, Y, movetoworkspacesilent, 6

# Move window between monitors
bind = $mainMod, M, exec, ags -r 'bind_monitor("+1")'
bind = $mainMod, N, exec, ags -r 'bind_monitor("-1")'
bind = $mainMod SHIFT, M, exec, ags -r 'migrate_clients()'
bind = $mainMod SHIFT, N, exec, ags -r 'bind_active_monitor()'

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, Up, workspace, e-1
bind = $mainMod, Down, workspace, e+1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Group windows
bind = $mainMod SHIFT, G, togglegroup

bind = $mainMod, U, changegroupactive, b
bind = $mainMod, I, changegroupactive, f


bind = $mainMod CTRL, H, movewindoworgroup, l
bind = $mainMod CTRL, L, movewindoworgroup, r
bind = $mainMod CTRL, K, movewindoworgroup, u
bind = $mainMod CTRL, J, movewindoworgroup, d

# resize mode
bind = $mainMod, S, submap, resize
submap = resize

binde = , h, resizeactive, -10 0
binde = , l, resizeactive, 10 0
binde = , k, resizeactive, 0 -10
binde = , j, resizeactive, 0 10

bind = , escape, submap, reset

submap = reset

# disable screen when close lid
bindl = , switch:off:Lid Switch, exec, hyprctl keyword monitor "desc:California Institute of Technology 0x1410,preferred,0x0,1.5"
bindl = , switch:on:Lid Switch, exec, hyprctl keyword monitor "desc:California Institute of Technology 0x1410,disable"
